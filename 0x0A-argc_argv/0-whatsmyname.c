#include "main.h"
/**
 * main -  program that prints its name, followed by a new line
 * @argc: function that prints the number of arguments passed into it
 * @argv: that prints its name
 * Return: 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
		printf("%s\n", *argv);
	return (0);
}
